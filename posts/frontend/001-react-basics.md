# React 基础入门

React 是一个用于构建用户界面的 JavaScript 库。它使用组件化的思想来管理界面。

## 核心概念

- **组件**: React 应用由多个组件组成
- **JSX**: 一种类似 HTML 的 JavaScript 扩展
- **状态**: 组件的内部数据
- **Props**: 组件的外部参数

## 简单示例

```jsx
function Welcome(props) {
  return <h1>Hello, {props.name}!</h1>;
}
```

这是 React 开发的第一步。

